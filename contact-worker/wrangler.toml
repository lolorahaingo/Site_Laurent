name = "contact-worker"
main = "src/index.js"
compatibility_date = "2024-01-01"

# La clé API Resend est stockée comme secret (pas en dur ici)
# Pour la configurer : wrangler secret put RESEND_API_KEY
